Yes, you're right. Replace TIMES_TARGETED with AVG_TIMES_PER_CLIENT:
Output:
MNE
YEAR_MONTH
UNIQUE_CLIENTS
AVG_TIMES_PER_CLIENT
VCN
2024-01
12,000
1.25
VCN
2024-02
8,500
1.20
VDA
2024-06
7,800
1.10
...
...
...
...
Code:
# %% Cell: VVD Campaign Summary by Month
vvd_mne = ['VCN', 'VDA', 'VDO', 'VDT', 'VUI', 'VUT', 'VAW']

# Filter to VVD only and add year_month
vvd_campaigns = client_campaigns \
    .filter(F.col("MNE").isin(vvd_mne)) \
    .withColumn("YEAR_MONTH", F.date_format(F.col("TREATMT_STRT_DT"), "yyyy-MM"))

# Summary
vvd_summary = vvd_campaigns.groupBy("MNE", "YEAR_MONTH").agg(
    F.countDistinct("CLNT_NO").alias("UNIQUE_CLIENTS"),
    F.count("*").alias("TOTAL")
).withColumn("AVG_TIMES_PER_CLIENT", F.round(F.col("TOTAL") / F.col("UNIQUE_CLIENTS"), 2)) \
 .drop("TOTAL") \
 .orderBy("MNE", "YEAR_MONTH")

vvd_summary.show(100, truncate=False)
This gives you unique clients per campaign per month, and how many times on average each client was targeted in that campaign/month.